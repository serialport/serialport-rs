[package]
name = "serialport"
version = "0.2.3"
authors = ["Bryant Mairs <bryant@mai.rs>"]
categories = ["hardware-support"]
keywords = ["serial", "hardware", "system", "RS232"]
license = "LGPL-3.0"
readme = "README.asciidoc"
repository = "https://gitlab.com/susurrus/serialport-rs"
description = "A cross-platform low-level serial port library"

[badges]
appveyor = { repository = "Susurrus/serialport-rs" }

[dev-dependencies]
argparse = "0.2"

[dependencies]
libc = "0.2.1"

[target."cfg(unix)".dependencies]
ioctl-rs = { path = "../ioctl-rs" }
termios = { path = "../termios-rs" }

[target.'cfg(target_os = "linux")'.dependencies]
libudev = "0.2.0"

[target."cfg(windows)".dependencies]
winreg = "0.3"
